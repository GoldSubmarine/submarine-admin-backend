package com.htnova.scaffold.modules.<%= module %>.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.htnova.scaffold.common.dto.XPage;
import com.htnova.scaffold.modules.<%= module %>.entity.<%= Entity %>;
import org.apache.ibatis.annotations.Param;

import java.util.List;

public interface <%= Entity %>Mapper extends BaseMapper<<%= Entity %>> {

    XPage<<%= Entity %>> findPage(XPage xPage, @Param("<%= entity %>") <%= Entity %> <%= entity %>);

    List<<%= Entity %>> findList(@Param("<%= entity %>") <%= Entity %> <%= entity %>);

}
